extends layout
block headtitle
  h2 Notice
block content
    style.
        #submits{
            display: block;
            margin:0 auto;
            padding:0 auto;
        }
        input,textarea{
            width: 100%;
            display:block;
        }
        input[type="file"]{
            margin:0 auto;
        }
        input[type="text"],textarea{
            weight:100%;
            overflow:scroll;
        }
        h1 {
            display:inline;
            
        }
        #cont {
            
            height:400px;
            font-size:18px;
            color:gray;
            
        }
    .wrapper
      #main.container
        // Content
        article#content
            #main.container
            header.memberTitle
              h2 공지사항 작성
            hr
            form(action="/notice/notice/write"  method="post")
                input(name="title" type="text" style="color:black;font-size:1.3em" value="공지사항 제목을 입력해주세요")
                hr
                textarea(name="content" id="cont" ) 공지사항 내용을 입력해주세요!
            
                //- label 파일 업로드
                //- input(type="file" name="attachment")           
                br
                button.btn(id="submits" type="submit") Submit